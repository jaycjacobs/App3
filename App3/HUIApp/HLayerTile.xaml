<UserControl
    x:Class="RedDog.HUIApp.HLayerTile"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RedDog.HUIApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="40"
    d:DesignWidth="400">

    <StackPanel x:Name="_grid" Orientation="Horizontal" Background="Transparent" VerticalAlignment="Stretch">
        <Grid x:Name="_sampleGrid" Background="{StaticResource BrightBackground}" VerticalAlignment="Stretch">
            <Line Width="50" VerticalAlignment="Center" Margin="6,0" x:Name="_line" 
                  X1="0" Y1="5" X2="50" Y2="5" Stroke="Black" StrokeThickness="1" UseLayoutRounding="False" />
        </Grid>
        <TextBlock x:Name="_layerName" Grid.Column="1" Grid.Row="0" Text="Layer Name" Foreground="Black" Margin="6,0,30,0" 
                   VerticalAlignment="Center" HorizontalAlignment="Stretch" />
    </StackPanel>
</UserControl>
