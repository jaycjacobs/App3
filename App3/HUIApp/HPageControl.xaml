<UserControl
    x:Class="RedDog.HUIApp.HPageControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RedDog.HUIApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="24"
    d:DesignWidth="400">
    
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <Button x:Name="_prevButton" Content="◀" Foreground="Black" FontSize="10" BorderThickness="0" Style="{StaticResource ButtonStyle1}" />
        <TextBlock x:Name="_label" Text="1-9 of 29" Foreground="Black" VerticalAlignment="Center" FontSize="12" Margin="0,5,0,0" />
        <Button x:Name="_nextButton" Content="▶" Foreground="Black" FontSize="10" BorderThickness="0" Style="{StaticResource ButtonStyle1}" />
    </StackPanel>
</UserControl>
